{
  "name": "sanbox-11ty-paginate-categories",
  "version": "1.0.0",
  "description": "Paginating categories with Eleventy",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf dist/",
    "build": "npx @11ty/eleventy --quiet",
    "dev": "npx @11ty/eleventy --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "rimraf": "^2.7.1",
    "slugify": "^1.5.0"
  }
}
